import { describe, expect, it } from 'vitest';

describe('Math tests', () => {
  it('adds 2 + 3 to equal 5', () => {
    expect(2 + 3).toBe(5);
  });

  it('subtracts 10 - 4 to equal 6', () => {
    expect(10 - 4).toBe(6);
  });
});